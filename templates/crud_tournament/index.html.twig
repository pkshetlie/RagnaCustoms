{% extends 'base.html.twig' %}


{% block title %}Custom tournaments{% endblock %}
{% block stylesheets %}
    {{ parent() }}
{% endblock %}
{% block body %}
<div class="container-fluid">

    <div class="row mt-3">
        <div class="col-md-2">
            {% include 'user/partial/_left_part_user.html.twig' %}
        </div>
        <div class="col-md-10">
            <h2>{{ "Custom tournaments"|trans }}</h2>
            <p></p>
            <div class="row">
                <div class="col-md-3 mb-5">
                    {% include 'crud_tournament/partial/card_plus.html.twig' %}
                </div>

        {% for tournament in tournaments %}
{#            {% include 'crud_tournament/partial/card.html.twig' %}#}
        {% endfor %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
